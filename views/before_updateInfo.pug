doctype html
html
    head
        meta(charset='utf-8')
        title 비밀번호 확인
        style
            include style.css
    body
        center
            img(src='/logo_transparent_cut.png' class='form_mini_logo' style='cursor:pointer;' onclick="location.href='/main'")
            h2 비밀번호 확인
            hr
            div(style='height: 10px;')
            form(name='beforeUpdateInfo_form' action='/beforeUpdateInfo' method='post')
                div
                    input(type='password' name='password' placeholder='Enter password' class='form_input')
                    div(style='height: 10px;')
                    button(type='submit' class='form_button') 확인