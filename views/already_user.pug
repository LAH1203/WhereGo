doctype html
html
    head
        meta(charset='utf-8')
        title Already User
        style
            include style.css
    body
        center
            img(src='/logo_transparent_cut.png' class='form_mini_logo' style='cursor:pointer;' onclick="location.href='/main'")
            h2 유저 정보가 있음
            hr
            div(style='height: 10px;')
            div
                button(type='button' class='form_button' onclick="location.href='/login'") 로그인하기