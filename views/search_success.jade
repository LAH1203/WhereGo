doctype html
html
    head
        meta(charset='utf-8')
        title Search Success
        style
            include style.css
    body
        div 검색 성공!
        -for(var i = 0; i < result.length; i++)
            div= result[i].PLACE
            div= result[i].CONTENT
            div= result[i].user_email
        div
            button(type='button' class='form_button' onclick="location.href='/main'") 메인으로 돌아가기