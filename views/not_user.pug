doctype html
html
    head
        meta(charset='utf-8')
        title Not User
        style
            include style.css
    body
        center
            img(src='/logo_transparent_cut.png' class='form_mini_logo' style='cursor:pointer;' onclick="location.href='/main'")
            h2 유저 정보가 없음
            hr
            div(style='height: 10px;')
            div
                button(type='button' class='form_button' onclick="location.href='/signup'") 회원가입하기