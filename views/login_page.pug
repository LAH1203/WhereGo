doctype html
html
    head
        meta(charset='utf-8')
        title Login
        style
            include style.css
        script(src='/login.js')
    body
        form(name='login_form' action='/process/login' method='get')
            center
                img(src='/logo_transparent_cut.png' class='form_mini_logo' style='cursor:pointer;' onclick="location.href='/main'")
                h2 로그인
                hr
                div(style='height: 10px;')
                div
                    div
                        a(class='form_item_name') ID
                    div
                        input(type='id' name='id' placeholder='Enter ID' class='form_input')
                    div(class='form_text_alert_padding')
                        div(id='alert_id' class='form_text_alert')
                div
                    div
                        a(class='form_item_name') Password
                    div
                        input(type='password' name='password' placeholder='Enter password' class='form_input')
                    div(class='form_text_alert_padding')
                        div(id='alert_password' class='form_text_alert')
                div
                    button(type='button' class='form_button' onclick='login()') 로그인
                div
                    button(type='button' class='form_button' onclick="location.href='/signup'") 회원가입